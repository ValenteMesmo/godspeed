module Colors

open Microsoft.Xna.Framework

let paper = Color(60,60,60)
let selection = Color.Red
let dark = Color(90,90,90)
let gray = Color(120,120,120)
let light = Color(180,180,180)
let eraser = Color.Transparent
